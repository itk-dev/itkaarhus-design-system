---
import Layout from '../../layouts/Layout.astro';
const base = import.meta.env.BASE_URL;
const file = 'examples/embed/website-landing.html';
---

<Layout title="Website landing page" bleed={true}>
  <iframe class="ex-frame" src={`${base}${file}`} title="Min Aarhus — landing page" loading="lazy"></iframe>
</Layout>

<style>
  .ex-frame { flex: 1; width: 100%; border: 0; background: var(--itkaarhus-white); }
</style>
